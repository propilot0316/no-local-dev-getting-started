<div class="container">

  <table>
    <tr>
      <th>First Name</th>
      <th>Last Name</th>
    </tr>

    <% @tasks.each do |task| %>
      <tr>
        <td><%= task.whoid %></td>
        <td><%= task.activitydate %></td>
      </tr>
    <% end %>
  </table>

</div>
